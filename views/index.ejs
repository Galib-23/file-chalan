<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <% if (locals.fileLink != null) { %>
    <div style="margin-bottom: 1rem; display: flex; flex-direction: column; align-items: center; gap: 10px;">
      <h2>Your file is uploaded</h2>
      <p>Share this link below</p>
      <a href="<%= locals.fileLink %>"> <%= locals.fileLink %> </a>
    </div>
    <% } %>
    <div class="header">
      <h1 class="header-title">File Chalan</h1>
      <p class="header-description">
        Transfer your files seamlessly with File chalan
      </p>
    </div>
    <form
      action="/upload"
      method="post"
      class="form"
      enctype="multipart/form-data"
    >
      <label for="file">File:</label>
      <input type="file" name="file" id="file" required />
      <label for="password">Password:</label>
      <input type="password" name="password" id="password" />
      <button class="button">Share</button>
    </form>
  </body>
</html>
